@page
@model ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Ошиибка.</h1>
<h2 class="text-danger">При выполнении Вашего запроса произошла какая-то ошибка. ОБратитесь к разработчику.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Request ID:</strong> <code>@Model.RequestId</code>
    </p>
}

